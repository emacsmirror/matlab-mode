#+startup: showall

* Executing commands from electric_indent_xr_start_pt_area_at_past_end.m:5:2:

  Case1: (t-utils-xr 

  (re-search-forward "^foo")
  (indent-region (save-excursion (re-search-backward "^a")) (save-excursion (re-search-backward "^x") (pos-eol)))

  (t-utils-xr-print-code (save-excursion (re-search-backward "^a")) (point-max))
  )

- Invoking      : (re-search-forward "^foo")
  Start point   :  280
  Moved to point:  350
  : 19:3: foo=2;
  :          ^
  No buffer modifications

- Invoking      : (indent-region (save-excursion (re-search-backward "^a")) (save-excursion (re-search-backward "^x") (pos-eol)))
  Start point   :  350
  Moved to point:  369
  : 19:3: foo=2;
  :          ^
  Buffer modified:
  #+begin_src diff
--- start_contents
+++ end_contents
@@ -11,9 +11,9 @@
   )
 %}
 
-a=1;
-bVar = [1 2 300   ];
-cLongVar = 10+f(3,4,    5)  ;
-x=2;
+a        = 1;
+bVar     = [1, 2, 300];
+cLongVar = 10 + f(3, 4, 5);
+x        = 2;
 
 foo=2;
  #+end_src diff

- Invoking      : (t-utils-xr-print-code (save-excursion (re-search-backward "^a")) (point-max))
  Start point   :  369
  No point movement
  standard-output:
  #+begin_src matlab-ts
a        = 1;
bVar     = [1, 2, 300];
cLongVar = 10 + f(3, 4, 5);
x        = 2;

foo=2;
  #+end_src
  No buffer modifications
