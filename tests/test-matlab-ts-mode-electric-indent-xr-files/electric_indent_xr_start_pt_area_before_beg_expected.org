#+startup: showall

* Executing commands from electric_indent_xr_start_pt_area_before_beg.m:5:2:

  Case1: (t-utils-xr 
  (re-search-backward "^%{")

  (indent-region (save-excursion (re-search-forward "^a") (pos-bol)) (save-excursion (re-search-forward "^x") (pos-eol)))

  (t-utils-xr-print-code (save-excursion (re-search-forward "^a") (pos-bol)) (point-max))
  )

- Invoking      : (re-search-backward "^%{")
  Start point   :  297
  Moved to point:   22
  : 3:0: %{ 
  :      ^
  No buffer modifications

- Invoking      : (indent-region (save-excursion (re-search-forward "^a") (pos-bol)) (save-excursion (re-search-forward "^x") (pos-eol)))
  Start point   :   22
  No point movement
  Buffer modified:
  #+begin_src diff
--- start_contents
+++ end_contents
@@ -11,7 +11,7 @@
   )
 %}
 
-a=1;
-bVar = [1 2 300   ];
-cLongVar = 10+f(3,4,    5)  ;
-x=2;
+a        = 1;
+bVar     = [1, 2, 300];
+cLongVar = 10 + f(3, 4, 5);
+x        = 2;
  #+end_src diff

- Invoking      : (t-utils-xr-print-code (save-excursion (re-search-forward "^a") (pos-bol)) (point-max))
  Start point   :   22
  No point movement
  standard-output:
  #+begin_src matlab-ts
a        = 1;
bVar     = [1, 2, 300];
cLongVar = 10 + f(3, 4, 5);
x        = 2;
  #+end_src
  No buffer modifications
